model RH {
  id            Int         @id @default(autoincrement())
  name          String
  raisonSociale String
  mail          String      @unique
  password      String
  siret         String
  employes      employe[]
  ordinateurs   ordinateurs[]
}