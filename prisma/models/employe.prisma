model employe {
  id          Int         @id @default(autoincrement())
  nom         String
  prenom      String
  mail        String      @unique
  password    String
  age         Int?
  genre       String?
  rhId        Int?
  rh          RH?         @relation(fields: [rhId], references: [id])
  ordinateurs  ordinateurs[]
}